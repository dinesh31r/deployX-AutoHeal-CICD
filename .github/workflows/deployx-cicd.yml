name: deployX CI/CD Pipeline

on:
  push:
    branches:
      - main

  workflow_dispatch:
    inputs:
      pipelineType:
        description: "Choose pipeline type"
        required: true
        default: "CI_ONLY"
        type: choice
        options:
          - CI_ONLY
          - CI_CD

jobs:
  ci:
    name: Continuous Integration
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Docker
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image (CI validation)
        run: |
          echo "Running CI pipeline"
          cd base-pipeline
          docker build -t deployx:autoheal -f docker/Dockerfile .

  cd:
    name: Continuous Deployment
    runs-on: ubuntu-latest
    needs: ci

    if: >
      github.event_name == 'workflow_dispatch' &&
      github.event.inputs.pipelineType == 'CI_CD'

    env:
      DOCKERHUB_USER: ${{ secrets.DOCKER_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKER_PASSWORD }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Debug - Check if secrets exist
        run: |
          if [ -z "$DOCKERHUB_USER" ]; then
            echo "❌ DOCKER_USERNAME secret is empty or not set"
            echo "Please add DOCKER_USERNAME to repository secrets"
            exit 1
          else
            echo "✅ DOCKER_USERNAME is set (value hidden)"
          fi

      - name: Login to Docker Hub
        run: |
          echo "$DOCKERHUB_TOKEN" | docker login -u "$DOCKERHUB_USER" --password-stdin

      - name: Build and Push to Docker Hub
        run: |
          cd base-pipeline
          docker build -t $DOCKERHUB_USER/deployx:latest -f docker/Dockerfile .
          docker push $DOCKERHUB_USER/deployx:latest
          echo "✅ Image pushed to Docker Hub: $DOCKERHUB_USER/deployx:latest"

      - name: Deployment Summary
        run: |
          echo "=========================================="
          echo "🚀 CD COMPLETED SUCCESSFULLY"
          echo "=========================================="
          echo "Image: $DOCKERHUB_USER/deployx:latest"
          echo "Registry: Docker Hub"
          echo "Triggered by: ${{ github.actor }}"
          echo "=========================================="
